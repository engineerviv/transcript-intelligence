# Copy this file to .env and fill in your values.
# The pipeline and API server both read from this file via python-dotenv.

# Required: OpenAI API key for transcript enrichment (GPT-4o-mini) and the chat agent
OPENAI_API_KEY=sk-...

# Optional overrides (defaults shown)
# FAISS_INDEX_PATH=outputs/faiss.index
# FAISS_META_PATH=outputs/faiss_meta.json
